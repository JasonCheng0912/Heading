8889
欢喜花生

ops：192.168.8.3
u41Diyl6f6vQoA1@WhdCcN5qDN

erp：192.168.5.3
8526qOGV%3rXexLJHQZmAM1HUa

slb：220.154.137.175





220.154.137.175:60501 erp测试机器 192.168.5.3
220.154.137.175:2222  ops机器 192.168.8.3





8526qOGV%3rXexLJHQZmAM1HUa
pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com
公网：cshxhs.rwlb.rds.aliyuncs.com

中间件：
redis：2.8
license-server：1.2.5
oss-mysql：5.7.14
rumba-oss-server：2.17.1
jposbo：5.7.14

需要数据初始化db-setup
hdpos4-dist: 2.18.6  对
h6-sop-service： 1.54
sos-h6-transfer: 1.70.6
hdpos6-notice-service: 2.0
mpa-service: 1.32
hdpos-oas-service: 1.39
pasoreport-web: 1.89.2
h6-openapi2-service: 1.51.5
panther: 1.100
card-server-proxy-service: csp_1.58
up-connector-service: 1.69.1
h6-cloudfund-service：1.37.1
h6-crm-service: 6.115


不需要数据初始化
gem-service: 2.10
init-tool: 2.4
jposbo: 关联 BO 立项任务单
panther-dts-server: 1.100
panther-taskweb: 1.100
spms-hdpos-web: 3.38.3
bpfm-app-service: 1.36.6
eaccount-server: 2.47.0
vss-spider-server：1.76.0
vss-spider-46：1.54.0
adi:1.7.6


、





"hdpos4-dist","h6-sop-service","sos-h6-transfer-service","hdpos6-notice-service","mpa-service","hdpos-oas-service","pasoreport-web","h6-openapi2-service","panther","card-server-proxy-service","up-connector-service","h6-cloudfund-service","h6-crm-service","init-tool","gem-service","jposbo","panther-dts-server","panther-taskweb","spms-hdpos-web","bpfm-app-service","eaccount-server","vss-spider-server","vss-spider-46","adi","redis","license-server","oss-mysql","rumba-oss-server"


return (appname.equals("hdpos4-dist"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("h6-sop-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("sos-h6-transfer-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("hdpos6-notice-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("hdpos-oas-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("pasoreport-web"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("h6-openapi2-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("panther"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("card-server-proxy-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("up-connector-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("h6-cloudfund-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("vss-spider-server"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("vss-spider-46"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("cadi-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:
       (appname.equals("h6-crm-service"))? ["jdbc:polardb2://pc-bp158678l039pjwpm.rwlb.rds.aliyuncs.com:1521/hdposcs?prepareThreshold=0\\\\\\&boolAsInt=true"]:[]



 

## erp.csv


```commandline
customer_code,project,host_id,c_version,c_tags,image_name,docker_repository,app_type,c_port,c_managerport,c_jmxport,c_health_timeout
8889,shhxhs,app-01,2.18.6,blue,hdpos4-dist,harbor.qianfan123.com/hdpos46/hdpos4-dist,backend,38180,39180,9744,45
8889,shhxhs,app-01,2.10,blue,gem-service,harbor.qianfan123.com/hdpos46/gem-service,backend,38105,39105,9787,15
8889,shhxhs,app-01,2.4,blue,init-tool,harbor.qianfan123.com/component/init-tool,backend,38093,0,0,15
8889,shhxhs,app-01,1.54,blue,h6-sop-service,harbor.qianfan123.com/hdpos46/h6-sop-service,backend,38172,39172,0,15
8889,shhxhs,app-01,1.70.6,blue,sos-h6-transfer-service,harbor.qianfan123.com/hdpos46/sos-h6-transfer-service,backend,38135,39135,9773,15
8889,shhxhs,app-01,2.0,blue,hdpos6-notice-service,harbor.qianfan123.com/hdpos46/hdpos6-notice-service,backend,38136,39136,0,15
8889,shhxhs,app-01,1.32,blue,mpa-service,harbor.qianfan123.com/hdpos46/mpa-service,backend,38186,39186,0,15
8889,shhxhs,app-01,1.39,blue,hdpos-oas-service,harbor.qianfan123.com/hdpos46/hdpos-oas-service,backend,38156,39156,0,15
8889,shhxhs,app-01,1.89.2,blue,pasoreport-web,harbor.qianfan123.com/component/pasoreport-web,backend,38980,39980,9756,45
8889,shhxhs,app-01,1.51.5,blue,h6-openapi2-service,harbor.qianfan123.com/hdpos46/h6-openapi2-service,backend,38176,39176,9768,15
8889,shhxhs,app-01,1.0,blue,jposbo,harbor.qianfan123.com/jposbo/jposbo,backend,38280,39280,10001,30
8889,shhxhs,app-01,1.100,blue,panther-dts-server,harbor.qianfan123.com/dts-store/panther-dts-server,backend,38480,39480,9755,30
8889,shhxhs,app-01,1.100,blue,panther-taskweb,harbor.qianfan123.com/dts-store/panther-taskweb,backend,38380,39380,9754,30
8889,shhxhs,app-01,csp_1.58,blue,card-server-proxy-service,harbor.qianfan123.com/component/card-server-proxy-service,backend,38119,39119,37119,15
8889,shhxhs,app-01,3.38.3,blue,spms-hdpos-web,harbor.qianfan123.com/component/spms-hdpos-web,backend,38115,39115,9752,15
8889,shhxhs,app-01,1.36.6,blue,bpfm-app-service,harbor.qianfan123.com/component/bpfm-app-service,backend,38212,39212,9757,15
8889,shhxhs,app-01,1.69.1,blue,up-connector-service,harbor.qianfan123.com/component/up-connector-service,backend,38110,39110,9758,15
8889,shhxhs,app-01,1.37.1,blue,h6-cloudfund-service,harbor.qianfan123.com/hdpos46/h6-cloudfund-service,backend,38197,39197,9807,15
8889,shhxhs,app-01,2.47.0,blue,eaccount-server,harbor.qianfan123.com/component/eaccount-server,backend,38173,0,0,15
8889,shhxhs,app-01,1.76.0,blue,vss-spider-server,harbor.qianfan123.com/vss/vss-spider-server,backend,38081,39081,0,15
8889,shhxhs,app-01,1.7.6,blue,adi,harbor.qianfan123.com/adi/adi,backend,38144,0,0,15
8889,shhxhs,app-01,6.115,blue,h6-crm-service,harbor.qianfan123.com/hdpos46/h6-crm-service,backend,38169,39169,0,15

```



##  hdpos.conf
```
server
{
    listen       80;
    server_name  localhost;
    access_log   /usr/local/openresty/nginx/logs/pos_access.log main;
    error_log   /usr/local/openresty/nginx/logs/pos_error.log;
    set_by_lua_file $traceid /usr/local/openresty/nginx/sbin/traceid.lua;
        
    location = /favicon.ico 
    {
        log_not_found off;
        access_log on;
    }
	
    location / {
        root   /usr/local/openresty/nginx/html;
        index  index.html index.htm;
    }
    
       location ~(/hdpos4-web/latin/file/downloadFile.hd|/hdpos4-web/static/gembox/) {
           proxy_pass http://hdpos4-dist;
           include ../conf.d/downloadfile.conf;
       }

    # hdpos4-dist
    location /hdpos4-web {
        access_log   /usr/local/openresty/nginx/logs/hdpos4-dist_access.log main;
        error_log   /usr/local/openresty/nginx/logs/hdpos4-dist_error.log;
        proxy_pass    http://hdpos4-dist;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

location ~* ^/rumba-oss-server/rs/oss/v1/[^/]+/o/[^/]+$ {
          if ($request_method != GET) {
              return 403;
          }

          proxy_pass http://rumba-oss-server;
          proxy_pass_header Server;
          proxy_set_header Host $host;
          proxy_redirect off;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          client_max_body_size 100m;
          client_body_buffer_size 128k;
          proxy_connect_timeout 30m;
          proxy_send_timeout 10m;
          proxy_read_timeout 30m;
          proxy_buffering on;
          proxy_buffer_size 128k;
          proxy_buffers 4 256K;
          proxy_busy_buffers_size 512k;
          proxy_max_temp_file_size 0;
          proxy_ignore_client_abort on;  
      }

      location ~* ^/rumba-oss-server/rs/oss/v1/[^/]+/o {
          return 403;
      }

	  # jposbo
  location /jposbo {
	         access_log   /usr/local/openresty/nginx/logs/jposbo_access.log main;
           error_log   /usr/local/openresty/nginx/logs/jposbo_error.log;

            proxy_pass    http://jposbo;
            proxy_pass_header Server;
            proxy_set_header Host $host;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Scheme $scheme;
            client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
          client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
          proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
          proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
          proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
          proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
          proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
          proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
          proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
          proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      set_real_ip_from 0.0.0.0/0;
      real_ip_header  X-Forwarded-For;
      real_ip_recursive on;
       }
# 配置pasoreport-web的反向代理
	location /pasoreport-web {
			proxy_pass    http://pasoreport;
			proxy_pass_header Server;
			proxy_set_header Host $host;
			proxy_redirect off;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Scheme $scheme;
			client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
		  client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		  proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		  proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		  proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		  proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		  proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		  proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		  proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		  proxy_max_temp_file_size 0;    #关闭磁盘缓冲
				  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
		}     
	location /h6-sop-service{
			proxy_pass    http://h6-sop-service;
			proxy_pass_header Server;
			proxy_set_header Host $host;
			proxy_redirect off;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Scheme $scheme;
			client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
		  client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		  proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		  proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		  proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		  proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		  proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		  proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		  proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		  proxy_max_temp_file_size 0;    #关闭磁盘缓冲
				  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
		}        
	location /sos-h6-transfer{
			proxy_pass    http://sos-h6-transfer;
			proxy_pass_header Server;
			proxy_set_header Host $host;
			proxy_redirect off;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Scheme $scheme;
			client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
		  client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		  proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		  proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		  proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		  proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		  proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		  proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		  proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		  proxy_max_temp_file_size 0;    #关闭磁盘缓冲
				  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
		}      
		
	location /mpa-service{
			proxy_pass    http://mpa-service;
			proxy_pass_header Server;
			proxy_set_header Host $host;
			proxy_redirect off;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Scheme $scheme;
			client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
		  client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		  proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		  proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		  proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		  proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		  proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		  proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		  proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		  proxy_max_temp_file_size 0;    #关闭磁盘缓冲
				  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
		}   
		
	location /hdpos-oas-service{
			proxy_pass    http://hdpos-oas-service;
			proxy_pass_header Server;
			proxy_set_header Host $host;
			proxy_redirect off;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Scheme $scheme;
			client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
		  client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		  proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		  proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		  proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		  proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		  proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		  proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		  proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		  proxy_max_temp_file_size 0;    #关闭磁盘缓冲
				  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
		}     
		
		
	location /h6-cloudfund-service{
			proxy_pass    http://h6-cloudfund-service;
			proxy_pass_header Server;
			proxy_set_header Host $host;
			proxy_redirect off;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Scheme $scheme;
			client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
		  client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		  proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		  proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		  proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		  proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		  proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		  proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		  proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		  proxy_max_temp_file_size 0;    #关闭磁盘缓冲
				  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
		} 
	location /h6-crm-service{
			proxy_pass    http://h6-crm-service;
			proxy_pass_header Server;
			proxy_set_header Host $host;
			proxy_redirect off;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Scheme $scheme;
			client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
		  client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		  proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		  proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		  proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		  proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		  proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		  proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		  proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		  proxy_max_temp_file_size 0;    #关闭磁盘缓冲
				  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
		} 		
		
		
		 
    # h6-openapi2-service
    location /h6-openapi2-service {
        access_log   /usr/local/openresty/nginx/logs/h6-openapi2-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/h6-openapi2-service_error.log;
        proxy_pass    http://h6-openapi2-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

    # init-tool
    location ~ (/init-tool-web|/inittool|/tempFile) {
        access_log   /usr/local/openresty/nginx/logs/init-tool_access.log main;
        error_log   /usr/local/openresty/nginx/logs/init-tool_error.log;
        proxy_pass    http://init-tool;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }
 
  

    # spms-hdpos-web
    location /spms-web {
        access_log   /usr/local/openresty/nginx/logs/spms-hdpos-web_access.log main;
        error_log   /usr/local/openresty/nginx/logs/spms-hdpos-web_error.log;
        proxy_pass    http://spms-hdpos-web;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

    # bpfm-app-service
    location /spms-web {
        access_log   /usr/local/openresty/nginx/logs/bpfm-app-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/bpfm-app-service_error.log;
        proxy_pass    http://bpfm-app-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }
    # hdpos6-notice-service
    location /notice {
        access_log   /usr/local/openresty/nginx/logs/hdpos6-notice-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/hdpos6-notice-service_error.log;
        proxy_pass    http://hdpos6-notice-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }
    
    
       location /eaccount-server{
                   proxy_pass    http://eaccount-server;
                   proxy_pass_header Server;
                   proxy_set_header Host $host;
                   proxy_redirect off;
                   proxy_set_header X-Real-IP $remote_addr;
                   proxy_set_header X-Scheme $scheme;
                   client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
                 client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
                 proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
                 proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
                 proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
                 proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
                 proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
                 proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
                 proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
                 proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                     proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
               }

       location /vss-spider-server{
                   proxy_pass    http://vss-spider-server;
                   proxy_pass_header Server;
                   proxy_set_header Host $host;
                   proxy_redirect off;
                   proxy_set_header X-Real-IP $remote_addr;
                   proxy_set_header X-Scheme $scheme;
                   client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
                 client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
                 proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
                 proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
                 proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
                 proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
                 proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
                 proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
                 proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
                 proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                     proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
               }

       location /vss-spider-46{
                   proxy_pass    http://vss-spider-46;
                   proxy_pass_header Server;
                   proxy_set_header Host $host;
                   proxy_redirect off;
                   proxy_set_header X-Real-IP $remote_addr;
                   proxy_set_header X-Scheme $scheme;
                   client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
                 client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
                 proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
                 proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
                 proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
                 proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
                 proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
                 proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
                 proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
                 proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                     proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
               }

       location /adi{
                   proxy_pass    http://adi;
                   proxy_pass_header Server;
                   proxy_set_header Host $host;
                   proxy_redirect off;
                   proxy_set_header X-Real-IP $remote_addr;
                   proxy_set_header X-Scheme $scheme;
                   client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
                 client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
                 proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
                 proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
                 proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
                 proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
                 proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
                 proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
                 proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
                 proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                     proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
               }
               
               
    # card-server-proxy-service
    location /cardserver {
        access_log   /usr/local/openresty/nginx/logs/card-server-proxy-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/card-server-proxy-service_error.log;
        proxy_pass    http://card-server-proxy-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

        location /panther-web {
            proxy_pass    http://panther-taskweb;
            proxy_pass_header Server;
            proxy_set_header Host $host;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Scheme $scheme;
            client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
          client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
          proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
          proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
          proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
          proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
          proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
          proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
          proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
          proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
       }

        location /panther-task-server {
            proxy_pass    http://panther-taskweb;
            proxy_pass_header Server;
            proxy_set_header Host $host;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Scheme $scheme;
            client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
          client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
          proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
          proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
          proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
          proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
          proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
          proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
          proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
          proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
       }
	   
        location /panther-dts-server {
            proxy_pass    http://panther-dts-server;
            proxy_pass_header Server;
            proxy_set_header Host $host;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Scheme $scheme;
            client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
          client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
          proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
          proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
          proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
          proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
          proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
          proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
          proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
          proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
       }

    # up-connector-service
    location /up-connector {
        access_log   /usr/local/openresty/nginx/logs/up-connector-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/up-connector-service_error.log;
        proxy_pass    http://up-connector-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }
	 location /gem-service {
		access_log   /usr/local/openresty/nginx/logs/gem-service_access.log main;
		error_log   /usr/local/openresty/nginx/logs/gem-service_error.log;
		proxy_pass    http://gem-service;
		proxy_pass_header Server;
		proxy_set_header Host $host;
		proxy_redirect off;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Scheme $scheme;
		client_max_body_size 500m;    #允许客户端请求的最大单文件字节数
		client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		proxy_max_temp_file_size 0;    #关闭磁盘缓冲
		proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
}			


 
  }
```
	

## upstream
```commandline
 
```
upstream hdpos4-dist {
      server 192.168.5.3:38180;
}

upstream h6-openapi2-service {
      server 192.168.5.3:38176;
}
upstream card-server-proxy-service {
      server 192.168.5.3:38119;
}
upstream up-connector-service {
      server 192.168.5.3:38110;
}
 
upstream sos-h6-transfer-service {
      server 192.168.5.3:38135;
}
upstream h6-sop-service {
      server 192.168.5.3:38172;
}
upstream vss-spider-server {
      server 192.168.5.3:38081;
}
upstream gem-service {
      server 192.168.5.3:38105;
}
upstream pasoreport-web {
      server 192.168.5.3:38980;
}
upstream hdpos6-notice-service {
      server 192.168.5.3:38136;
}
upstream h6-crm-service {
      server 192.168.5.3:38169;
}
 
upstream mpa-service {
      server 192.168.5.3:38186;
}
upstream hdpos-oas-service {
      server 192.168.5.3:38156;
}
upstream h6-cloudfund-service {
      server 192.168.5.3:38197;
}
upstream init-tool {
      server 192.168.5.3:38093;
}
upstream jposbo {
      server 192.168.5.3:38280;
}
upstream panther-dts-server {
      server 192.168.5.3:38480;
}
upstream panther-taskweb {
      server 192.168.5.3:38380;
}
upstream spms-hdpos-web {
      server 192.168.5.3:38115;
}
upstream bpfm-app-service {
      server 192.168.5.3:38212;
}
upstream eaccount-server {
      server 192.168.5.3:38173;
}
upstream adi {
      server 192.168.5.3:38144;
}
upstream rumba-oss-server {
      server 192.168.5.3:38112;
}



Function允许提交事务的配置
// show polar_enable_function_commit; (alter role all set polar_enable_function_commit = on;)
on

显示游标不支持Commit
// show polar_plsql_enable_holdable_refcursor; (alter role all set polar_plsql_enable_holdable_refcursor = on;)
on

解决调用function报错问题
// show polar_enable_call_function_syntax; (alter role all set polar_enable_call_function_syntax = on;)
on

兼容性配置
// show polar_enable_stmt_transaction_rollback; (alter role all set polar_enable_stmt_transaction_rollback = on;)
on

// 兼容Oracle的RowId配置
// show polar_default_with_rowid; (alter role all set polar_default_with_rowid = on;)
on 

// 解决function无法抛出no_data_found错误
// show polar_enable_select_into_return_null; (alter role all set polar_enable_select_into_return_null = off;)
off 

// 打开错误码映射
// show polar_enable_ora_sqlcode; (alter system for cluster set polar_enable_ora_sqlcode to on;)
on

// 允许execute immediate
// show polar_enable_commit_in_execute_immediate; (alter system for cluster set polar_enable_commit_in_execute_immediate = on;)
on 

// 函数同义词
// show polar_enable_correct_candidate_of_function_with_synonym; (alter system for cluster set polar_enable_correct_candidate_of_function_with_synonym = on;)
on

// 关闭因执行脚本过程中产生的SQL错误异常退出
// show polar_enable_check_cursor_return_early; (alter role all set polar_enable_check_cursor_return_early = off;)
off

-- cron的时区(需要重启数据库才能生效，目前只能在控制台修改。)
需业务人员确认时区需要定义什么
cron.timezone = 'PRC';


pg_cron定时任务插件(polardb2.0没有默认安装)
create extension pg_cron;


全局临时表运维工具插件(每个数据库都需要单独执行)
create extension polar_gtt;


PolarDB数据库手册：https://gitlab.hd123.com/HDPOS4.6/docs/-/blob/master/dev/PolarDB数据库手册.md
