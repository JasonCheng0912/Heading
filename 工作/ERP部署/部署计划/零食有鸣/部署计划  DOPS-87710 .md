DOPS-87710

8560
 
namespace_admins: 'shujun,yaobohai,zhaochenhao,chengjiashuo,bairuiwen,dengwen,huangyanfei,lichuanjiang,liuminghao,qileiheng,zhangjinsong,zhaowenyu'
project_admins: 'shujun,yaobohai,zhaochenhao,chengjiashuo,bairuiwen,dengwen,huangyanfei,lichuanjiang,liuminghao,qileiheng,zhangjinsong,zhaowenyu'

公网域名：erp.uatnew.cdlsym.com

公网IP： 8.137.188.20

172.20.64.87  8C 64G
172.20.64.88  8C 64G
172.20.64.82  8C 32G
Lsym@2026

```commandline
需要数据初始化db-setup

hdpos4-dist: 1.89  
panther 1.65.33 
vbs-service ：1.15 
h6-openapi2-service：1.22.lsym.40
card-server-proxy-service：2.33.3 
up-connector-service：1.43.7
lsym-service：1.42
sos-h6-transfer-service：1.41.45
 

pasoreport-web：1.60.11
hdpos6-notice-service ：1.11 

h6-yzvcm-service：1.6.3
h6-crm-service：1.9.2
openapi-doc-service：1.22.lsymnew.42.alpha.2022608131440
不需要数据初始化
gem-service：2.7
panther-dts-server   1.65.33
panther-taskweb  1.65.33
spms-hdpos-web：3.36.4
bpfm-app-service：1.11.5
mkh-mas-transfer-server：1.26.0.2
mkh-mas-transfer-servertenant：1.38.0

sas-h4-transfer ：1.30.1.1 否
erp-mas2-transfer-server ：1.1.9  否
ras-h4-transfer：1.13.1  否
init-tool: 1.6.12
h6-ias-transfer：1.6.1
jposbo：lsym_2026072.11_pro

```

## project-app.yml
```commandline

                    return (host.equals('172.20.64.87'))? ["hdpos4-dist","hdpos6-notice-service","h6-crm-service","h6-yzvcm-service","panther-dts-server","panther-taskweb","spms-hdpos-web","bpfm-app-service","openapi-doc-service","sas-h4-transfer","erp-mas2-transfer-server","ras-h4-transfer","init-tool","gem-service","h6-ias-transfer","redis","license-server","oss-mysql","rumba-oss-server","mysql"]:
                    (host.equals('172.20.64.88'))? ["h6-openapi2-service","vbs-service","card-server-proxy-service","up-connector-service","lsym-service","sos-h6-transfer-service","h6-sop-service","vss-spider-server","vss-spider-46","pasoreport-web","mkh-mas-transfer-server","mkh-mas-transfer-servertenant"]:[]
 ```

## project-db.yml
```commandline



"hdpos4-dist","vbs-service","h6-openapi2-service","panther","card-server-proxy-service","up-connector-service","lsym-service","sos-h6-transfer-service","h6-sop-service","vss-spider-server","vss-spider-46","gem-service","pasoreport-web","hdpos6-notice-service","h6-crm-service","h6-yzvcm-service"
```

```commandline
return (appname.equals("hdpos4-dist"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("vbs-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("h6-openapi2-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("panther"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("card-server-proxy-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("up-connector-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("lsym-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("sos-h6-transfer-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("h6-sop-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("vss-spider-server"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("vss-spider-46"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("gem-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("pasoreport-web"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("hdpos6-notice-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("h6-crm-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:
       (appname.equals("h6-yzvcm-service"))? ["jdbc:oracle:thin:@172.20.64.84:1521:hdposcs"]:[]
       

```
  

 
CRM版本号：6.113.0                                                                                                                                                                  




 中间件： 

   - redis：必须部署。版本为：2.8
  - license-server：必须部署。版本为：1.2.5
  - oss-mysql：如果部署oss则必须要部署此中间件。版本为5.7.14
  - rumba-oss-server：如果部署spms组件才需要部署此中间件。版本为：2.17.1
  -  jposbo需要mysql(5.7.14) 
  
## erp.csv
```
customer_code,project,host_id,c_version,c_tags,image_name,docker_repository,app_type,c_port,c_managerport,c_jmxport,c_health_timeout
 

##  hdpos.conf
```
server
{
    listen       80;
    server_name  localhost;
    access_log   /usr/local/openresty/nginx/logs/pos_access.log main;
    error_log   /usr/local/openresty/nginx/logs/pos_error.log;
    set_by_lua_file $traceid /usr/local/openresty/nginx/sbin/traceid.lua;
        
    location = /favicon.ico 
    {
        log_not_found off;
        access_log on;
    }
	
    location / {
        root   /usr/local/openresty/nginx/html;
        index  index.html index.htm;
    }
    
       location ~(/hdpos4-web/latin/file/downloadFile.hd|/hdpos4-web/static/gembox/) {
           proxy_pass http://hdpos4-dist;
           include ../conf.d/downloadfile.conf;
       }

    # hdpos4-dist
    location /hdpos4-web {
        access_log   /usr/local/openresty/nginx/logs/hdpos4-dist_access.log main;
        error_log   /usr/local/openresty/nginx/logs/hdpos4-dist_error.log;
        proxy_pass    http://hdpos4-dist;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

location ~* ^/rumba-oss-server/rs/oss/v1/[^/]+/o/[^/]+$ {
          if ($request_method != GET) {
              return 403;
          }

          proxy_pass http://rumba-oss-server;
          proxy_pass_header Server;
          proxy_set_header Host $host;
          proxy_redirect off;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          client_max_body_size 100m;
          client_body_buffer_size 128k;
          proxy_connect_timeout 30m;
          proxy_send_timeout 10m;
          proxy_read_timeout 30m;
          proxy_buffering on;
          proxy_buffer_size 128k;
          proxy_buffers 4 256K;
          proxy_busy_buffers_size 512k;
          proxy_max_temp_file_size 0;
          proxy_ignore_client_abort on;  
      }

      location ~* ^/rumba-oss-server/rs/oss/v1/[^/]+/o {
          return 403;
      }

	  # jposbo
  location /jposbo {
	         access_log   /usr/local/openresty/nginx/logs/jposbo_access.log main;
           error_log   /usr/local/openresty/nginx/logs/jposbo_error.log;

            proxy_pass    http://jposbo;
            proxy_pass_header Server;
            proxy_set_header Host $host;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Scheme $scheme;
            client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
          client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
          proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
          proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
          proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
          proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
          proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
          proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
          proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
          proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      set_real_ip_from 0.0.0.0/0;
      real_ip_header  X-Forwarded-For;
      real_ip_recursive on;
       }
# 配置pasoreport-web的反向代理
	location /pasoreport-web {
			proxy_pass    http://pasoreport;
			proxy_pass_header Server;
			proxy_set_header Host $host;
			proxy_redirect off;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Scheme $scheme;
			client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
		  client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		  proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		  proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		  proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		  proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		  proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		  proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		  proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		  proxy_max_temp_file_size 0;    #关闭磁盘缓冲
				  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
		}        

    # h6-openapi2-service
    location /h6-openapi2-service {
        access_log   /usr/local/openresty/nginx/logs/h6-openapi2-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/h6-openapi2-service_error.log;
        proxy_pass    http://h6-openapi2-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

    # init-tool
    location ~ (/init-tool-web|/inittool|/tempFile) {
        access_log   /usr/local/openresty/nginx/logs/init-tool_access.log main;
        error_log   /usr/local/openresty/nginx/logs/init-tool_error.log;
        proxy_pass    http://init-tool;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }
 
    # gem-service
    location /gem-service {
        access_log   /usr/local/openresty/nginx/logs/gem-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/gem-service_error.log;
        proxy_pass    http://gem-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 500m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

    # spms-hdpos-web
    location /spms-web {
        access_log   /usr/local/openresty/nginx/logs/spms-hdpos-web_access.log main;
        error_log   /usr/local/openresty/nginx/logs/spms-hdpos-web_error.log;
        proxy_pass    http://spms-hdpos-web;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

    # hdpos6-notice-service
    location /notice {
        access_log   /usr/local/openresty/nginx/logs/hdpos6-notice-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/hdpos6-notice-service_error.log;
        proxy_pass    http://hdpos6-notice-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

    # card-server-proxy-service
    location /cardserver {
        access_log   /usr/local/openresty/nginx/logs/card-server-proxy-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/card-server-proxy-service_error.log;
        proxy_pass    http://card-server-proxy-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }

        location /panther-web {
            proxy_pass    http://panther-taskweb;
            proxy_pass_header Server;
            proxy_set_header Host $host;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Scheme $scheme;
            client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
          client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
          proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
          proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
          proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
          proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
          proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
          proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
          proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
          proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
       }

        location /panther-task-server {
            proxy_pass    http://panther-taskweb;
            proxy_pass_header Server;
            proxy_set_header Host $host;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Scheme $scheme;
            client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
          client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
          proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
          proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
          proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
          proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
          proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
          proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
          proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
          proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
       }
	   
        location /panther-dts-server {
            proxy_pass    http://panther-dts-server;
            proxy_pass_header Server;
            proxy_set_header Host $host;
            proxy_redirect off;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Scheme $scheme;
            client_max_body_size 100m;    #允许客户端请求的最大单文件字节数
          client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
          proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
          proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
          proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
          proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
          proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
          proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
          proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
          proxy_max_temp_file_size 0;    #关闭磁盘缓冲
                  proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
       }

    # up-connector-service
    location /up-connector {
        access_log   /usr/local/openresty/nginx/logs/up-connector-service_access.log main;
        error_log   /usr/local/openresty/nginx/logs/up-connector-service_error.log;
        proxy_pass    http://up-connector-service;
        proxy_pass_header Server;
        proxy_set_header Host $host;
        proxy_redirect off;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Scheme $scheme;
        client_max_body_size 100m;
        client_body_buffer_size 128k;
        proxy_connect_timeout 30m;
        proxy_send_timeout 10m;
        proxy_read_timeout 30m;
        proxy_buffering on;
        proxy_buffer_size 128k;
        proxy_buffers 4 256K;
        proxy_busy_buffers_size 512k;
        proxy_max_temp_file_size 0;
        proxy_ignore_client_abort on;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        set_real_ip_from 0.0.0.0/0;
        real_ip_header X-Forwarded-For;
        real_ip_recursive on;
    }
	 location /gem-service {
		access_log   /usr/local/openresty/nginx/logs/gem-service_access.log main;
		error_log   /usr/local/openresty/nginx/logs/gem-service_error.log;
		proxy_pass    http://gem-service;
		proxy_pass_header Server;
		proxy_set_header Host $host;
		proxy_redirect off;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Scheme $scheme;
		client_max_body_size 500m;    #允许客户端请求的最大单文件字节数
		client_body_buffer_size 128k;  #缓冲区代理缓冲用户端请求的最大字节数，
		proxy_connect_timeout 30m;  #nginx跟后端服务器连接超时时间(代理连接超时)
		proxy_send_timeout 10m;        #后端服务器数据回传时间(代理发送超时)
		proxy_read_timeout 30m;         #连接成功后，后端服务器响应时间(代理接收超时)
		proxy_buffering on;                   # 是否打开后端响应内容的缓冲区
		proxy_buffer_size 128k;             #设置代理服务器（nginx）保存用户头信息的缓冲区大小
		proxy_buffers 4 256K;               #proxy_buffers缓冲区，网页平均在32k以下的话，这样设置
		proxy_busy_buffers_size 512k;    #高负荷下缓冲大小（proxy_buffers*2）
		proxy_max_temp_file_size 0;    #关闭磁盘缓冲
		proxy_ignore_client_abort on;         #如果客户端断开请求,也保持与后端服务器的连接，防止服务器出现BUG
}			


# rumba-oss-server
	location ~* ^/rumba-oss-server/rs/oss/v1/[^/]+/o/[^/]+$ {
	  if ($request_method != GET) {
		  return 403;
	  }

	  proxy_pass http://rumba-oss-server;
	  proxy_pass_header Server;
	  proxy_set_header Host $host;
	  proxy_redirect off;
	  proxy_set_header X-Real-IP $remote_addr;
	  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	  client_max_body_size 100m;
	  client_body_buffer_size 128k;
	  proxy_connect_timeout 30m;
	  proxy_send_timeout 10m;
	  proxy_read_timeout 30m;
	  proxy_buffering on;
	  proxy_buffer_size 128k;
	  proxy_buffers 4 256K;
	  proxy_busy_buffers_size 512k;
	  proxy_max_temp_file_size 0;
	  proxy_ignore_client_abort on;  
  }

  location ~* ^/rumba-oss-server/rs/oss/v1/[^/]+/o {
	  return 403;
  }
```
	
	
}


## upstream.conf
```
upstream hdpos4-dist {
      server 10.249.83.8:38180;#hdpos4-web应用的服务器ip和端口
}

upstream hdpos6-notice-service {
      server 10.249.83.8:38136;  #notice应用的服务器ip和端口
}

upstream card-server-proxy-service {
    server 10.249.83.8:38119;
}

upstream pasoreport-web {
      server 10.249.83.8:38980;#pasoreport-web应用的服务器ip和端口
}

upstream up-connector-service {
      server 10.249.83.8:38110;  #up-connector应用的服务器ip和端口
}

upstream h6-openapi2-service {
      server 10.249.83.8:38176;
}

upstream panther-taskweb {
      server 10.249.83.8:38380;  #panther-web/task-server访问用
}

upstream panther-dts-server {
      server 10.249.83.8:38480;  #panther-dts-server, 
}

upstream init-tool {
    server 10.249.83.8:38093;  #init-tool应用的服务器ip和端口
}

upstream gem-service {
          server 10.249.83.8:38105;#gem-service应用的服务器ip和端口
}

upstream spms-hdpos-web {
      server 10.249.83.8:38115;#spms-web应用的服务器ip和端口
}

upstream jposbo {
          server 10.249.83.8:38280;#jposbo应用的服务器ip和端口
}

upstream rumba-oss-server {
      server 10.249.83.8:38112;
}
```


## 遇到问题

yum install python3-pip -y

pip3 install docker


cat /hdapp/heading/*/apollodir/wiki*